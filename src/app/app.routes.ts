import { Routes } from '@angular/router';
import {GoogleoauthComponent}  from './googleoauth/googleoauth.component'

export const routes: Routes = [
    {path:  'dashboard', component: GoogleoauthComponent}
];
